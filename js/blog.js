const search=document.getElementById('articleSearch');const buttons=document.querySelectorAll('.filter');const cards=document.querySelectorAll('.article-card');let cat='all';function run(){const q=(search?.value||'').toLowerCase();cards.forEach(c=>{const okCat=cat==='all'||c.dataset.category===cat;const okQ=c.innerText.toLowerCase().includes(q);c.classList.toggle('hide',!(okCat&&okQ));});}search?.addEventListener('input',run);buttons.forEach(btn=>btn.addEventListener('click',()=>{buttons.forEach(x=>x.classList.remove('active'));btn.classList.add('active');cat=btn.dataset.filter;run();}));